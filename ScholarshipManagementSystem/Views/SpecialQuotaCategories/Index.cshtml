@model IEnumerable<DAL.Models.Domain.MasterSetup.SpecialQuotaCategory>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CategoryName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CategoryType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PercentageValue)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CategoryName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CategoryType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PercentageValue)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SpecialQuotaCategoryId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SpecialQuotaCategoryId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SpecialQuotaCategoryId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

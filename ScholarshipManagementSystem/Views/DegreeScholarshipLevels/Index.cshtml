@model IEnumerable<ScholarshipManagementSystem.Models.Domain.MasterSetup.DegreeScholarshipLevel>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/_Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsActive)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Enrollment)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Threshold)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SchemeLevel)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DegreeLevel)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsActive)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Enrollment)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Threshold)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SchemeLevel.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DegreeLevel.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DegreeScholarshipLevelId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DegreeScholarshipLevelId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DegreeScholarshipLevelId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
